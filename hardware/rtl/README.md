# RTL

Future portable synthesizable hardware description sources belong here. Keep FPGA primitives, board transport adapters, and generated build outputs outside this directory.

Do not add tensor-accelerator implementation until the v0 workload and numerical gates in `docs/haslab-v0-contract.md` are resolved.
