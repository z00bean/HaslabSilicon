# GitHub configuration

This directory holds repository automation and contribution templates. The workflow validates repository structure and tests the Python numerical model, functional command simulator, and independent candidate conformance corpus. It does not build RTL or verify FPGA/ASIC hardware.
