# Reference-model tests

The standard-library `unittest` suite covers numeric conversions, exhaustive FP8 encoding round trips, exact integer arithmetic, overflow, layouts, activations, pooling, upsampling, convolution, matrix multiplication, argmax, and the v0 sampling rejection.

Run it with `make test` from the repository root.
