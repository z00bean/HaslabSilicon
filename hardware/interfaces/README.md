# Hardware interfaces

This directory will contain machine-readable interface definitions once the draft v0 ABI is reviewed and frozen. Until then, the authoritative proposal is `docs/haslab-v0-contract.md`.
