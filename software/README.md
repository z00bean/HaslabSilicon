# Software

This directory is reserved for future host utilities and, later, RISC-V firmware support. Public runtime code will live in `runtime/`; graph compilation in `compiler/`.

No firmware or host software is implemented.
