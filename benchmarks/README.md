# Benchmarks

This directory will hold workload manifests, export/calibration recipes, evaluation protocol, and machine-readable results. It must not contain unlicensed model weights, datasets, or unverifiable performance claims.

The initial proposed workload is YOLOv8n at batch one, 320×320. It has not yet been exported, calibrated, or benchmarked in this repository.
