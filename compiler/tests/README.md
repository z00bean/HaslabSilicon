# Compiler tests

The tests cover deterministic `.hxb` framing and required-section validation. The cross-component synthetic Conv-SiLU execution test is under `runtime/tests/`; the pinned third-party model is exercised by the reproducible benchmark tool without checking its derived weights into Git.

Checked-in test inputs must remain small, license-cleared, and reproducible.
