# GitHub configuration

This directory holds repository automation and contribution templates. The current workflow validates only repository structure; it does not build or test accelerator functionality.
