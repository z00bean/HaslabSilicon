# Command-simulator tests

These tests cover command framing, sequence and FIFO behavior, memory transfers, alignment and bounds faults, accumulator chunk lifecycle, all three epilogue modes, utility operations, reset, and successful END completion.

Run only this suite with `make sim`, or all Python verification with `make test`.
